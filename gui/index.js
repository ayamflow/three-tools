import { addShaderGUI } from './shader'

// import Controlkit from 'controlkit'
// const GUI = new Controlkit().addPanel()
// if (!sniffer.isDesktop || process.env.ENV != 'dev') GUI.disable()
// GUI.disable()

export default {
    addShader:  function(shader, options) {
        // addShaderGUI(GUI, shader, options)
    }
}